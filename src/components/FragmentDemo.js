import React from 'react'

export default function FragmentDemo() {
  return (
    <React.Fragment >
      <h1>Fragment Demo</h1>
      <p>This is using fragments to avoid unnecessary div's. Key attribute is the only attribute passed to react fragments</p>
    </React.Fragment>
  )
}
